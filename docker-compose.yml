version: '3.9'

services:
  app:
    build: .
    container_name: deepfake_detection_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./videos:/app/videos
      - ./data:/app/data
    environment:
      - FLASK_ENV=development
    command: ["python", "app.py"]
